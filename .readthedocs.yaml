version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .  # Isso instala o seu código para o Sphinx ler os sensores

sphinx:
  configuration: docs/source/conf.py