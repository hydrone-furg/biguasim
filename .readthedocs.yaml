version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - requirements: biguasim_docs/requirements.txt
    - method: pip
      path: .  # Isso instala o seu código para o Sphinx ler os sensores

sphinx:
  configuration: biguasim_docs/conf.py